Add a work-in-progress item to progress tracking.

If text provided in arguments, use it. Otherwise ask what's in progress.

Run:
```bash
claude-harness progress wip "<item_description>"
```

Arguments: $ARGUMENTS
