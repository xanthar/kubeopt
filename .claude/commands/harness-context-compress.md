Compress the current session to save context.

This will:
1. Generate a handoff document
2. Archive the current session
3. Reset context tracking

Confirm with me, then run:
```bash
claude-harness context compress
```
