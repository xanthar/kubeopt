Add a blocker to progress tracking.

If text provided in arguments, use it. Otherwise ask what the blocker is.

Run:
```bash
claude-harness progress blocker "<blocker_description>"
```

Arguments: $ARGUMENTS
