Run the project initialization/startup script.

Run:
```bash
claude-harness run
```

This executes `scripts/init.sh` (or `init.ps1` on Windows) which typically:
- Checks environment
- Shows git status
- Starts the application
- Runs tests
