Generate an E2E test scaffold for a feature.

If feature ID provided, use it. Otherwise, ask which feature.

Run:
```bash
claude-harness e2e generate <feature_id>
```

Show the generated test file location.

Arguments: $ARGUMENTS
