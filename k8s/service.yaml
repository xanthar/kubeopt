apiVersion: v1
kind: Service
metadata:
  name: kubeopt-ai
  namespace: kubeopt
  labels:
    app: kubeopt-ai
    app.kubernetes.io/name: kubeopt-ai
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app: kubeopt-ai
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kubeopt-ai
  namespace: kubeopt
  labels:
    app: kubeopt-ai
